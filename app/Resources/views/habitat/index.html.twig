{% extends 'base.html.twig' %}

{% block body %}
    <h1>habitat list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Namehabitat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for habitat in habitats %}
            <tr>
                <td><a href="{{ path('habitat_show', { 'id': habitat.id }) }}">{{ habitat.id }}</a></td>
                <td>{{ habitat.nameHabitat }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('habitat_show', { 'id': habitat.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
