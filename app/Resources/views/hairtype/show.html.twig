{% extends 'base.html.twig' %}

{% block body %}
    <h1>hairType</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hairType.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hairType.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('hairtype_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
